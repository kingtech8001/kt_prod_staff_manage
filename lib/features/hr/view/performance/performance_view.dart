import 'package:flutter/material.dart';

class HrPerformanceView extends StatelessWidget {
  const HrPerformanceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('HR Performance')));
  }
}
